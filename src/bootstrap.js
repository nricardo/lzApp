'use strict';

// import angular2-now
import {bootstrap} from 'angular2-now';

// load top level component (main)
import {lzApp} from 'components/lz/lz';

// load our css framework
import styles from 'bootstrap.scss';

bootstrap(lzApp);
